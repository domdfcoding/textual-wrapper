# This file is managed by 'repo_helper'. Don't edit it directly.
---
name: "Docs Check"
on:
  push:
    branches-ignore:
      - 'repo-helper-update'
      - 'pre-commit-ci-update-config'
      - 'imgbot'
  pull_request:

jobs:
  docs:
    permissions:
      contents: read
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: "actions/checkout@v4"

      - name: Install and Build 🔧
        uses: sphinx-toolbox/sphinx-action@sphinx-3.3.1-py310

        with:
          pre-build-command: |
            python -m pip install tox
            apt update
            apt install libgirepository1.0-dev libgtk-3-dev libvte-2.91-0 gir1.2-vte-2.91 libdbusmenu-glib4 gir1.2-dbusmenu-glib-0.4 libunity9 gir1.2-unity-5.0 libcairo-gobject2 libcairo2-dev pkg-config cmake -y
          docs-folder: "doc-source/"
          build-command: "tox -e docs -- -W "
